apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: events-postgres-data
  namespace: events-api
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: nfs-fast-retain
  resources:
    requests:
      storage: 5Gi
